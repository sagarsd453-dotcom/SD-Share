{
  "name": "sd-share",
  "version": "1.0.0",
  "description": "Real-time P2P file sharing platform",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "start": "concurrently \"npm start --prefix frontend\" \"npm start --prefix backend\"",
    "install-all": "npm install --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
